import 'package:flutter/material.dart';

class CounselPage extends StatelessWidget {
  const CounselPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Service currently inavailabe'));
  }
}
